## [v3.5] — 2026-04-18 — Footer Visibility Fix + Modular Refactor

Footer "reveal-from-behind" pattern'inde içerik kesilmesi (RC-1, RC-3, RC-4, RC-8, RC-9) çözüldü ve `Footer.tsx` 488 → 157 satıra indirildi (180 satır limiti uyumlu).

### Added
- `src/components/footer/footerLinks.ts` — link grupları için merkezi data modülü
- `src/components/footer/FooterBrand.tsx` — marka kimliği + iletişim bloğu
- `src/components/footer/FooterBackdrop.tsx` — watermark + grid + radial glow dekoratif katmanı (clamp font-size + max-w-full overflow guard)
- `src/components/footer/FooterNewsletter.tsx` — glass-morphism newsletter card
- `src/components/footer/FooterCTA.tsx` — final CTA card
- `src/components/footer/FooterBottomBar.tsx` — telif + legal linkler (lg:pr-20 floating chat rezervi)
- `docs/footer-debug/issue1-snapshot-before.txt` — ISSUE-1 regresyon guard snapshot

### Changed
- **`Footer.tsx`** (488 → 157 satır):
  - **RC-1/RC-3 (Spacer ölçümü):** `offsetHeight` → `scrollHeight` geçişi. `ResizeObserver` + `document.fonts.ready.then(updateHeight)` + `requestAnimationFrame` wrapper ile font swap / lazy image sonrası reflow yakalanıyor. Layout thrashing önlemi: `lastH` cache + diff check.
  - **RC-4 (Z-index):** Footer reveal variant `zIndex: 30` (önceden 0) → `finalCta` (z=24) üzerinde garanti.
  - **RC-8 (Bottom bar overflow):** `flex-col lg:flex-row` + `flex-wrap` + `lg:pr-20` → 1280px altında copyright/legal linkler kesintisiz, sağ alt floating chat butonu için 80px rezerv.
  - **RC-9 (Mobile horizontal overflow):** Watermark `clamp(40px, 12vw, 160px)` + `max-w-full overflow-hidden`; newsletter form container `min-w-0`.
- **Memory:** `mem://design/footer-reveal-pattern` v3.4/v3.5 kuralları ile güncellendi (scrollHeight, fonts.ready, z=30, lg:pr-20 chat rezervi, RC-9 watermark guard).

### Fixed
- Ana sayfa (`/`) reveal variant footer'ında alt bar + son link satırının kesilmesi → çözüldü (375/768/1280 PASS).
- `/malzemeler`, `/iletisim` static variant'ında 1280px altı yatay overflow → çözüldü.

### Validation
- `npx tsc --noEmit` → 0 errors
- Tüm yeni footer alt componentleri < 180 satır
- ISSUE-1 (`/malzemeler/aluminyum`) snapshot diff temiz

